# Progress

Snapshot of the current state. Update this file in place when the state changes; history lives in `log.md`.

## Current state (2026-09-20)

Live at delaybahn.com: FastAPI + DuckDB serve the bahn.de journey search enriched with delay history (30 full days, DE+AT+NL+CH+FR+IT) from the file-backed `data/delays.duckdb` the pipeline materializes (in-memory parquet load kept as fallback; 2026-08-07 — startup <1 s / ~0.5 G RSS instead of ~45 s / 6.7 G peak, which had OOM-looped the 8 GB production box); vanilla-JS frontend. Hosted on a Hetzner VPS since 2026-08-06 (CX33 — 4 vCPU / 8 GB / 80 GB — since the in-place rescale of 2026-08-29; previously ps083 since 2026-07-26, still the dev box): `delaybahn-pipeline.timer` (05:30 Europe/Berlin) runs the day-incremental DE→AT→NL→CH→FR→IT→merge flow and restarts the app; the FR GTFS-RT, AT ÖBB-HAFAS, NL OVapi-GTFS-RT and IT ViaggiaTreno pollers run 24/7 under systemd; systemd OnFailure hooks + an external 5-min health watchdog push outage alerts via ntfy (2026-08-07).

- **Delay stats** — median arrival delay per leg (since 2026-07-19) over a selectable 7/15/30-day window (default 7); per-day chart behind each badge. Chart tooltips, a tap/click bubble, and past-mode badges name the IRIS delay cause (DE legs only; 2026-07-26). Opening a chart drops a brief wordless tap-nudge on its tallest bar — the click-gesture icon with a white ripple pulsing at its fingertip, self-fading after ~6 s, removed on the first tap, hidden under prefers-reduced-motion (2026-08-31). CH/FR coverage since 2026-07-20, AT (top ~200 stations) since 2026-08-14 and NL (whole network) + IT (ViaggiaTreno-tracked runs) since 2026-08-15, so cross-border journeys get badges and warnings on every leg. German S-Bahn legs get badges since 2026-08-02 (matched by line label — bahn.de sends "S5" as fahrtNr, not a run number); U-Bahn/tram/city-bus/ferry legs show "nicht erfasst" instead of "keine Daten" (no IRIS data exists; lookup skipped via `UNTRACKED_PRODUCTS`).
- **Live (echtzeit) times** (2026-08-03, user-verified in browser) — same-day connections show bahn.de's real-time departure/arrival wherever it deviates from the schedule: struck-out scheduled time next to the live time in red (like bahn.de) in the journey header and on each leg row, header duration switches to the re-planned `ezVerbindungsDauerInSeconds`. Transfer-feasibility gaps (`_transfer_pairs`) use live times, so a connection that only works because the next train is itself delayed isn't flagged — and a live-shrunken gap is. Past mode keeps pure schedule times (the actual delay is the badge/simulation's story).
- **Return journey** (2026-08-09, Playwright-verified) — the search mask offers an optional return date/time, inert until "+ Rückfahrt hinzufügen" is clicked, so the one-way flow is untouched. With a return set the results become three steps: the outbound list swaps its booking button for "Weiter / Continue", picking one collapses it into a summary bar with "Ändern / Change" and searches the return direction starting at that journey's planned arrival; the return cards carry "Weiter" too, and picking one opens step 3 — both journeys in full with their delay badges and day charts, the combined "ab" price, and one button that opens bahn.de with both dates in the mask (`rd` + `rza=D`). The two legs read as separate blocks: an uppercase chip carrying the direction arrow and the step number ("→ 1. Hinfahrt" / "← 2. Rückfahrt"), the route on its own headline line, and a plain divider between them — no colour, which the user rejected 2026-08-09. bahn.de has no link format that pre-selects the two trains themselves (the `vbid` deep link pins one connection, must be minted server-side by DB, and has no round-trip form); a note under the button used to spell that out, removed 2026-08-09 as clutter, so the caveat now lives only here and in log.md. Every step tile behind the current one is a button back to that step (backwards only — going forward would need a pick that has not been made; inert while a leg is loading). Return options that would leave before the outbound lands are dropped (bahn.de answers a departure query with a window reaching slightly behind the requested time), and the earlier-connections button pages back only as far as that arrival, retiring there. Since 2026-08-13 the search **preflights the return leg**: the outbound list is shown only once the return direction has been fetched at the requested time (its own 150 s retry budget and its own "Rückfahrt noch nicht abrufbar" wording), and that proven answer is handed to step 2, so picking an outbound normally costs no second upstream call. Hidden in past mode.
- **Stopovers** (2026-08-23; folded into the advanced-options panel 2026-08-25, headless-Chrome-verified desktop + 390 px; every control on the panel's lines — selects, the Verkehrsmittel picker and the dashed add buttons — measures a uniform 41 px since 2026-08-26, so their top edges line up and not just their bottoms) — up to two Zwischenhalte per direction as the last row of the advanced-options panel ("+ Zwischenhalt hinzufügen" reveals a row: station + minimum stay on a bahn.de-style `− 01ʰ : 45ᵐⁱⁿ +` stepper since 2026-08-26 — the buttons snap to the quarter hour, both segments stay typeable on desktop, phones get the native time picker the search form's time field uses, and older bare-minute links still restore), sent to bahn.de as `zwischenhalte: [{id, aufenthaltsdauer}]` (format live-verified against `/angebote/fahrplan` before wiring: Berlin→München 0/5 via Frankfurt baseline, 7/7 with the field). Round trips carry separate outbound/return stopover groups and the return preflight includes them; vias join the journeys cache key and the route-level stale key so degraded answers never cross via sets; URL round-trips through `vo*`/`vr*` params (panel restored open); hidden in past mode with the panel; the booking deep link does not yet carry stopovers (bahn.de mask param unverified).
- **Transport-mode filter** (2026-08-23; folded into the advanced-options panel and restyled to match the selects 2026-08-25, headless-Chrome-verified desktop + 390 px) — bahn.de's ten Verkehrsmittel groups behind a picker beside Umstiegszeit that reads "Alle" or the enabled count; a modal carries the ten toggles plus Alle / Nur Nahverkehr / Nur Fernverkehr quick rows, and only "Übernehmen" commits. Sent as a narrowed `produktgattungen`, which also drops `schnelleVerbindungen` — that pruning is what would hide the slower trains the filter asked for. The set joins the journeys cache key and the route-level stale key; round trips filter each half independently; `tm`/`tmr` round-trip through the URL and `vm=` codes ride the booking deep link; neutralised in past mode with the rest of the panel. Composes with stopovers (ICE-only via Frankfurt verified live).
- **Transfer risk** — a transfer with ≤ 2 min slack after the arriving leg's median delay gets an inline strip under the leg and a yellow "⚠ Knapper Umstieg" header pill (next to it the median badge shows only when red, since 2026-07-27); when the median delay exceeds the transfer time by > 30 min, a red "⛔ Anschlussrisiko" pill replaces the median badge and the journey is demoted in the least-delay sort (browser-verified 2026-07-26). Since 2026-08-03 each tight-transfer strip carries a "Falls verpasst: Ankunft HH:MM" disclosure button showing the next realistic connection to the destination if the transfer is missed (server-computed per tight transfer, one cached bahn.de replan each); expanding it crosses out the legs that would no longer happen (Playwright-verified 2026-08-03: catchability rule, toggle/cross-out, earliest-open-miss-wins with two panels, day chart inside the panel, 390 px no-overflow).
- **Sorting** — departure, least delay, cheapest price, lowest connection risk (by header-pill tier; within a tier by `minTransferMargin`, the riskiest transfer's slack, since 2026-07-26 — comparator-tested only), fewest transfers (2026-07-26, Playwright-verified).
- **Compensation checker** (2026-07-23, browser-verified) — lives on its own indexable sub-page `/entschaedigung` since 2026-08-03: the home CTA (below the hero chart since 2026-07-26) and the permanent header pill are plain links to it; the server rewrites index.html per request with past-specific SEO head tags + `<h1>`, legacy `?mode=past` URLs redirect with params preserved. A searched past journey shows each leg's exact delay that day, is simulated leg by leg (missed/cancelled connections re-planned via bahn.de, missed legs struck out, the actual continuation shown), and gets DB Fahrgastrechte compensation (25 %/50 % vs the booked arrival); the claim button opens a step-by-step modal — the journey as it appears in the bahn.de past-trips list plus replicas of the buttons to click there — whose continue link opens the bahn.de trip overview (2026-07-31). Too-recent dates say when to check back.
- **Live same-day** (2026-07-25, verified on live data) — a journey taken today is checkable minutes after arrival: for days past the parquet, `app/live_delays.py` reads IRIS `plan` + `fchg` at request time (measured 14–17 h lookback meets the next morning's build); unreported legs show "noch offen / pending" with the claim withheld. DE-only; inert without credentials.
- **Frontend/site** — site-wide DE/EN toggle (since 2026-08-15 the two languages are separate indexable URLs, not one page a toggle rewrites: German keeps `/` and `/entschaedigung`, English is `/en/` and `/en/compensation`; the pills are real links to the counterpart page, language comes from the path, each variant carries its own title/description/canonical/hreflang/og/JSON-LD, and the English pages ship English markup server-side — `app/main.py` lifts the plain strings out of `I18N.en` in app.js and substitutes them into every `data-i18n` element, so there is no second copy of the translations; a returning visitor who picked English is redirected from the German URL by a localStorage-gated head script that crawlers never trigger), shareable search URLs, earlier/later paging (each click emits a `page` Umami event with `{dir, leg}` since 2026-08-13, to size a possible prefetch; since 2026-09-13 one page per visit is free and the next press opens a short account modal — login button parks the press and a scroll anchor (the topmost card in view + its offset), `/login?reason=page`, replayed on return on the same leg and scrolled back to the same spot; "Jetzt nicht" and a backdrop tap close it, signed-in visitors are never asked, frontend nudge only), autocomplete answered from the local delay data (each suggestion row carries a ☆/★ favourite toggle since 2026-08-14 — favourites head the empty-field list above the recent stations, one unlabelled list, `favoriteStations` in localStorage; not browser-verified; long station names wrap to a second line instead of truncating behind the star since 2026-08-15), a ✕ clear control inside each Von/Nach field while it holds text (visibility is CSS `:placeholder-shown`, so programmatic fills — ⇅ swap, URL restore — track too; clearing nulls the picked station, keeps focus and reopens the favourites/recents list; out of the tab order; 2026-08-15, Playwright-verified at desktop + 390 px), hero chart leads with a red headline ("Setz auf den pünktlicheren Zug.") over the in-page claim (plot behind a toggle; headline added 2026-08-07; graphs and month strings cover the two most recent complete months — July–August 2026 since 2026-09-20 — refreshed by `pipeline/update_monthly_graphs.py`, which regenerates the SVGs, patches every month-dependent string and bumps all busters; monthly cron on ps083 since 2026-09-20, see the maintenance list), topped by a dot—line—dot route glyph since 2026-08-15 — it replaced a rounded-end red rule that resembled DB's "Puls" brand element, and the CSS brand vars were renamed `--db-red` → `--brand-red` the same day (zero visual change; value stays #fd1c17), SEO meta/robots/sitemap (2026-07-26; /entschaedigung added to the sitemap 2026-08-03; site name declared as "DelayBahn" in every signal Google reads — title/og:site_name/WebSite JSON-LD in the head, PAST_TITLE, and the four i18n pageTitle strings that overwrite document.title at render time — 2026-08-15, after Google showed "delaybahn.com" as both site name and title link; all four meta descriptions lead with the brand name and the footer trademark disclaimer is wrapped in `data-nosnippet` since 2026-08-17, after Google used that disclaimer as the entire homepage snippet — a description that never said "DelayBahn" was discarded as irrelevant to the query, leaving the disclaimer as the only brand-bearing sentence in the crawlable body; Search Console re-index request pending user action), Impressum & Datenschutzerklärung page footer-linked in both languages (2026-07-27), its privacy half re-audited against the login/report code 2026-08-26 (six corrections; the page itself is still German-only), Ko-fi donate link — muted footer link plus a one-line nudge shown only after a successful search, plain links, no widget, never both in one view (the footer link hides while the nudge shows, 2026-07-27); donate asks currently disabled via the `DONATE_ENABLED` kill switch in app.js (2026-07-28); footer trademark disclaimer in both languages (independent project, DB marks belong to Deutsche Bahn AG; 2026-07-28; its text sits in a `data-nosnippet` span since 2026-08-17 so Google cannot quote it as a search snippet — still fully visible on the page, and the `data-i18n` hook moved onto the span so both translation paths still reach it); a `Copyright © 2026 Shashank Tripathi` line closes the footer on both the homepage and the Impressum (2026-08-17) — holder taken from the Impressum's § 5 DDG operator, year from the repo's first commit, and deliberately given no `data-i18n` key since a name and a year read the same in both languages; the word plus the real `©` rather than `Copyright (c)`, which is the ASCII-source-header convention and carries no legal weight of its own; **follow links** to the X/LinkedIn/Instagram company pages since 2026-08-17, in the two places that can actually convert — three unlabelled 20 px monochrome brand marks (official Simple Icons paths, CC0, in that order; the original Instagram-first sequence carried no rationale and was reversed on request, and since the ask's row is a clone of the footer node the order lives in one place) opening the footer above the Open Source line on both the homepage and the Impressum, and the same row cloned into the feedback ask's own line between 👎 and ✕, labelled "New features:" behind a hairline divider (English in both languages by user choice; the icons already carry the "follow" half, so the label only supplies the why). The footer alone was judged a poor acquisition surface — it sits below ~500 px of train diorama — while `#feedback-nudge` is the one element that appears at the end of a fresh result list. One copy of the marks lives in the footer markup and `mountFollowRow()` clones it at init, building its own label span; the anchors carry `data-i18n-title`/`data-i18n-aria` and hardcoded German `title`/`aria-label` but never `data-i18n`, since both `_translate()` and `applyLang()` replace such an element's whole content with plain text and would drop the `<svg>`. Hover darkens to `--text` instead of tinting third-party brand marks brand-red. Delegated `follow-*` Umami events cover the clone; `sameAs` on the WebSite JSON-LD node and `twitter:site`/`twitter:creator` = `@DelayBahn` wire the accounts into the metadata, and the Impressum's "Externe Links" paragraph now names the three networks; decorative full-width CSS/SVG toy-train scene above the footer — loco + coaches roll in, pause 1.5 s at a centered station under a red "+75 min" board, wheels synced to the drive keyframes, `prefers-reduced-motion` static pose, language-neutral so no i18n changes (2026-07-31; the same markup block closes the Delay Stories page too since 2026-08-26 — every `.ts-*` rule already lives in the shared stylesheet, so that was 78 lines of markup and nothing else); on ≤ 700 px screens the journey-card header re-lays as a grid — times/price on the top row, badges right-aligned, full-width booking/claim button — and the sort pills wrap instead of forcing page-wide horizontal scroll (2026-08-02); the Datum/Uhrzeit inputs no longer overflow the search card on iOS Safari (native appearance stripped, value left-aligned, 2026-08-02); journey legs render on a vertical timeline rail — a circle per train leg joined by a gray line so connections read at a glance, walks on the bare line, warning strips with a pass-through segment, past-mode continuation on its own rail run (2026-08-02, pixel-scan-verified in headless Chrome at desktop + 400 px widths), circles and rail caps anchored to each row's first line (`--stop-y`) so wrapped mobile rows keep them aligned with the train chip (2026-08-02); clickable delay badges carry a full-size ▾ disclosure caret that flips while the day chart is open (`aria-expanded`-driven), with an :active press state and a larger mobile tap target, so the tap-for-chart feature is discoverable on touch (2026-08-02); two mutually exclusive D-Ticket switches in the search mask, over bahn.de's two independent search flags — "Nur D-Ticket" restricts results to Deutschland-Ticket-valid connections, and since 2026-08-21 "D-Ticket + alle Züge" declares the ticket but keeps every connection, so bahn.de reprices each journey for a ticket holder: fully covered ones come back priceless with an `MDA-NUR-DT` meldung and show the green "D-Ticket" label in the price slot, mixed ones are repriced for the paid legs only (Berlin → Leipzig ICE+S-Bahn 55,99 € → 49,99 €) and keep their price with a tooltip naming what it covers; `schnelleVerbindungen` is dropped for that mode alone, because the fast-connection preference prunes exactly the slower regional options the ticket pays for (München → Augsburg returned 5 paid ICEs and 0 covered connections with it on); covered journeys count as €0 in the cheapest-price sort and the round-trip total; the booking deep link carries the mode over (`dltv` alone, or `dlt`+`dltv`), the state round-trips through the shareable URL (`dticket=all`/`only`, legacy `dticket=1` still read as `only`), and both toggles are hidden and force-disabled in past mode because D-Ticket journeys carry no Fahrgastrechte compensation (2026-08-03, second mode 2026-08-21); the two switches share one line with the search button beside them as a single wrapping block, shrinking on ≤ 420 px so they still fit at 360 px and stacking to a full-width button on ≤ 700 px (2026-08-21, Playwright-verified 1280→320 px in both languages) — since 2026-08-23 the two switches fold away with the new **passenger age selector** behind an "Erweiterte Optionen" chevron toggle beside the search button (chevron glyph 11 → 18 px since 2026-08-24 — at 11 px the ▸ was near-invisible on phones; a restored URL with a non-default setting opens the panel, past mode hides toggle and panel outright): searches price for any of bahn.de's five single-traveler age brackets (27-64 default, 65+, 15-26, child 6-14, toddler 0-5), mapped to the vendo `reisende` typ (`FAMILIENKIND`/`KLEINKIND` for the child brackets, per bahn.de's stammdaten), keyed into both journeys caches and validated server-side, carried into the bahn.de deep link via `r=<typ>:16:KLASSENLOS:1`, past mode pinning adult; a lone toddler travels free, so those priceless rows show "kostenfrei / free" instead of "Preis auf bahn.de"; the select carries no visible label (aria-label only — the option texts name the bracket); after a tapped search settles, the page smooth-scrolls the outcome into view — the trip stepper, sort controls or status line, so result lists, “no connections” and errors are all seen — unless it already sits in the top half of the viewport; button tap only, so toggle refetches, paging and URL-restored searches don’t scroll (2026-08-13, on-phone check after deploy pending).
- **Visitor feedback + contact** (2026-08-10, Playwright-verified) — a "Hat dir das geholfen? 👍 👎 ✕" ask under every fresh result list (single search, both round-trip legs, the compensation check; mutually exclusive with the donate nudge, which is off): vote → optional comment → thanks. Restyled 2026-08-17 after the ask read as too subtle to notice and the one-line box produced one-word comments: a hairline `border-top` instead of a muted 12 px footnote (a full white card was built and rejected as too prominent for the slot), a 4-row resizable `<textarea>` instead of a 320 px `<input>`, and a placeholder that asks for specifics instead of the old "Optional, ein Satz genügt" / "one sentence is plenty" — the copy was asking for exactly the short comments that prompted the change. Enter now inserts a newline, so Ctrl/Cmd+Enter submits; multi-line comments need nothing downstream (`max_length` is on the string, SQLite stores TEXT verbatim, ntfy gets the raw POST body). `POST /api/feedback` stores vote/text/lang/context in the app's own `data/feedback/feedback.db` SQLite (`app/feedback.py` — the delays DuckDB is read-only and swapped nightly; nothing identifying stored, a per-prompt sid merges vote + comment into one row, per-IP in-memory throttle 5/h, write off the event loop); written comments push via ntfy when `NTFY_TOPIC` is in the service env — which it was not until 2026-08-14, so every comment between 2026-08-10 and then was stored but never pushed (20 of them, delivered as one digest after the fix; `notify()` now warns rather than returning silently, and checks the ntfy response status). Umami gets one event name per outcome since 2026-08-14 (`feedback-up`/`feedback-down`/`feedback-text`/`feedback-dismiss`), because the single `feedback` event hid the vote two dropdowns deep in the Properties tab; `feedback-shot` joined 2026-08-18. Since 2026-08-18 a comment can carry a **screenshot** — an attach button under the textarea or a straight Ctrl/Cmd+V paste into it (placeholder says so), client-shrunk to ≤ 500 KB (canvas downscale to 1600 px + JPEG quality ladder; small PNGs kept as-is), sent as a base64 data URL in the same JSON POST, re-validated server-side (mime allowlist, strict base64, 512 KiB cap, magic bytes), stored as a `shot BLOB` (auto-migrated) under the same sid-merge rules, and pushed to ntfy as a second attachment-only message. Abuse guards, same date: an ASGI middleware 413s `POST /api/feedback` bodies over 1 MiB before they are buffered (411 without Content-Length), and a rolling-24 h 200 MB screenshot storage budget in `save()` drops images (keeping vote + text, and skipping the ntfy image push) once full, raising a high-priority ntfy warning at most every 6 h — the per-IP throttle cannot stop distributed bots, so this bounds disk growth; a free-plan Cloudflare rate-limiting rule (`feedback-flood`: 10 POSTs/10 s per IP on `/api/feedback`, block 10 s, dashboard-only config) stops single-IP floods at the edge. Screenshots are user-chosen images and may contain personal data — no impressum wording for that yet. No quiet window: each new result list re-arms the ask with a fresh sid — the original 30-day localStorage window was removed on user request 2026-08-10 (it had made the ask look missing from the other flows after a single test vote); the impressum documents the processing. Separately, a footer "Kontakt / Contact us" link is a plain `mailto:` to kontakt@delaybahn.com with the subject prefilled, opening the visitor's mail app directly — since 2026-08-17, replacing a dialog that collected the message body first: the mail client has its own compose box, and the JS-assembled address it used to hide was already published plainly in the impressum. A click listener still fires the `contact` Umami event without preventing the navigation.
- **Delay Stories** (worktree built 2026-08-18/20, landed 2026-08-24) — a community board at `/geschichten` (DE) / `/stories` (EN): station horror stories with comments and Reddit-style up/down votes (since 2026-08-24: one signed vote per account, net score, the opposite arrow switches and the same arrow clears) and a Reddit-style Share menu — Copy link to a per-story permalink (`/stories/{id}`, the board with that story pinned and its meta in the head) or Embed as an iframe card (`/embed/stories/{id}`) (2026-08-24), a tap-to-count damage-report board (problem tiles with a where/when form behind the first tap; opens on the year span since 2026-09-01 — the month view zeroes out at every month boundary), accounts on Firebase Authentication (shipped 2026-08-26: Google, Apple, phone or email through the Firebase JS SDK on `/login` - the email path is a six-digit code we mail via Brevo and redeem in Firestore for a Firebase custom token, since Firebase has no email OTP, with a password offered as the fallback; the server verifies the ID token sent as a bearer and stores no login data at all — a Firestore registry keeps usernames unique and the claimed name rides in the token as the `handle` custom claim; this replaced the 2026-08-24 Brevo magic-link accounts, whose rows `scripts/import_users_to_firebase.py` carries over as `legacy-<id>` uids). Per-language URLs served with the same server-side translation as `/en/` (`_en_strings()` generalized), own meta/hreflang/sitemap entries, footer-linked site-wide. Content in `data/stories/stories.db` (stories, comments, votes, problem_reports, keyed by Firebase uid) — wiped 2026-08-24 for a fresh launch; the only server-side account config is `FIREBASE_SA_FILE` (service-account JSON, not in the repo), `/health` reports `"auth": {"configured": …}`, and Firebase being unreachable is a 503 on account endpoints while reading stays up. Brevo is still the relay, now only for the six-digit code mails; its free-plan credits are watched hourly behind `BREVO_API_KEY` and reported in `/health` under `"mail"`. Umami: pageviews + action events (post/comment/vote/report/sort/login funnel), `/verify` tracker excludes the token via `data-exclude-search`. Announced on the homepage since 2026-08-24 by the `#stories-cta` card (square logo web copy, NEU pill, one-line pitch, per-language link) below the refund CTA at the foot of the page (moved there 2026-08-26; it opened above the CTA when it shipped 2026-08-24); hides during a search and in past mode. Since 2026-08-26 a second, deliberately thin entry point sits at the very top of `<main>` under the install prompt: `#stories-banner`, one ~40 px row of wide wordmark + pitch + `→` linking to the board, its href and logo rewritten per language server-side like the card's; the muted second sentence drops under 420 px and the whole row hides in past mode. Since 2026-08-27 it carries the install prompt's dismiss cross (`#stories-banner-dismiss`, sharing the `.install-dismiss` rule): the row became a `<div id="stories-banner">` holding an `<a class="stories-banner-link">` — a button may not nest inside an anchor — and the ✕ stores `storiesBannerDismissed` on a 15-day re-arm (2026-08-27; the install prompt's `installPromptDismissed` went 60 → 30 days the same day) — and since 2026-08-27 the row keeps the install prompt's 12 px side padding on phones too, where a `7px 10px` rule used to leave the two crosses 2 px out of column, the click event moving onto the link so the cross is not counted as a banner click. Since 2026-08-26 the page header is the site-wide one again (DelayBahn logo linking home, page title + tagline); the Delay Stories wordmark stands on its own, centred at the top of `<main>`, its `src`/`alt` rewritten per language server-side (`STORIES_WORDMARK`). Since 2026-08-26 the **homepage header carries the same account corner** (`.header-auth`: Anmelden link, or username + Abmelden), between the page title and the compensation pill — the account is site-wide, so the search page can sign in and shows who is signed in; app.js paints it from the lookup `initReports()` already does, so no extra SDK download, and the login link stamps `?next=<current path+query>` at click time because `syncUrl()` rewrites the query as the visitor searches. Since 2026-09-06 the header CTA reads just „Entschädigung“ / “Delay Compensation” and a „Meine Fahrten“ / “My trips” pill sits beside it in a shared nav.header-pills unit (wraps as a pair, per-language hrefs, trips-nav Umami event; past mode grays only the compensation pill). Since 2026-09-17 a **section strip** (`<nav class="site-nav">`) sits under the red header on every page — Verbindungssuche · Entschädigung beantragen · Verspätungs-Rangliste ("Neu" chip) · Delay Geschichten, text links with the current page underlined in brand red, sideways-scrolling on phones (where the Verbindungssuche link shrinks to a house icon, its label kept for screen readers); `_site_nav()` in app/main.py fills in the language's paths and labels and marks the active page for every server-rendered page, login.js does it for the static login page, impressum keeps the German strip; the compensation pill left the homepage header for the strip, "Meine Fahrten" stays there as the account link. The homepage header is two rows on phones, so its logo keeps the full 60 px there — the ≤700 px `.logo { height: 48px }` shrink is now scoped to the login/impressum headers and to the stories header, which since 2026-08-26 wraps the same way — title and tagline beside the logo on row 1, the account corner and the DE/EN switch right-aligned on row 2 (`.header-stories`), instead of squeezing the title to a word per line.
- **Story translation** (2026-09-20; two real calls verified, toggle not browser-rendered) — every story and comment is machine-translated between German and English once, in the background, by `app/translate.py` (Claude, `claude-opus-5`, `TRANSLATE_MODEL` overrides; needs `ANTHROPIC_API_KEY`, a no-op without it) and cached in `stories.db` against a hash of the source, so an edit re-translates and a removal forgets. Reads carry `lang=`; a post in the other language shows its translation marked "machine-translated · Show original", in the list, the comments, the top strip, the permalink's head and the embed card. The first reader of an untranslated post sees the original. Only title and text are sent, over a direct connection; the privacy page names the processing.
- **Country leaderboard** (built 2026-08-25, restyled and extended 2026-09-04/05) — `/rangliste` (DE) / `/leaderboard` (EN): every country in the delay table ranked by the share of arrivals under 6 min late over the last day / 7 / 30 days (month is the landing view), with average delay, cancellations, stops and a 30-day sparkline; a podium for the top three; an atlas-style map of Europe (Natural Earth via `scripts/build_europe_map.py` → `static/europe-map.js`, no map library and no runtime download) with the six countries as parchment cut-outs tinted by delayed share and labelled with rank and figure; a table whose metric headings re-sort and re-number the rows, with on-time bars coloured green/yellow/red on a fixed scale per table (2026-09-18: long-distance red at 50 % → green at 97 %, all trains red at 80 % → green at 97 %, each the span its six countries cover; one shared window left one table or the other in a single colour), and a method note behind a `<details>` toggle; on phones the table keeps all seven columns and scrolls sideways under sticky rank + country columns instead of hiding cancellations/stops/trend, and the sort headers carry `touch-action: manipulation` with ~32 px touch targets (2026-09-05). `/api/leaderboard` is one grouped DuckDB scan cached per data version and warmed at startup, re-polled every 5 min while the page is open. A second table under the first (2026-09-13) ranks long-distance trains only, defined by train category per country (`leaderboard.LONG_DISTANCE`: ICE/IC/EC, Railjet, Nightjet, TGV/Ouigo, Intercity and equivalents; no distance data exists, so ">100 km" is a gloss), with its own sort state. Since 2026-09-17 the page follows the long-distance ranking (podium, map, tooltips, first table) and the all-trains table sits last as "Alle Züge" / "All trains"; the map ramp runs 0–50 % delayed so DE/IT/AT no longer saturate to one shade. The hero (2026-09-17) opens with the red "Europas Verspätungs-Rangliste" / "Europe's Delay Leaderboard" wordmark (train + podium mark, `static/leaderboard-logo-{de,en}.png`, src/alt swapped per language server-side; it replaced an uppercase text kicker the same day) and a two-tone serif question as the H1 — since the logo landed, the page accent `--lb-accent` (H1 italic, map rank badges, sparkline dots) is the site's brand red rather than the earlier orange, so hero and wordmark share one red ("Wer fährt pünktlich, wer lässt warten?" / "Who runs on time, and who keeps you waiting?"), system serif stack, no webfont. At the foot of the page (2026-09-17) a "Mehr Daten oder Auswertungen gewünscht? / Hier anfragen." card: a textarea whose request goes to `/api/feedback` as vote `request`, context `leaderboard` (stored with the thumbs, ntfy title "DelayBahn analysis request"); the server requires a signed-in, verified account for that vote, the page keeps the box visible to everyone and on Send without an account shows a quiet inline "log in to send – your text will be kept" line with a login link, parking the draft in sessionStorage for the way back (`/login?reason=request&next=…#lb-request`). The thanks line promises to share the data or analysis over email once it exists (2026-09-17, replacing "it will show up on the leaderboard"); since later the same day the request row also stores the account's uid and email (nullable columns, auto-migrated; thumbs stay anonymous) and the ntfy body opens with `From: <email> [<uid>]`, so the promise can be kept — the impressum's Feedback section documents it (Art. 6 Abs. 1 lit. b, deletion on request only, no automatic purge by the user's decision). Since 2026-09-18 the page ends like the homepage: the animated toy-train scene between the request box and the footer, and the X / LinkedIn / Instagram icon row at the top of the footer (both copied verbatim from index.html; Delay Geschichten, which already had the train, got the icon row the same day). Also since 2026-09-18 the hero carries one link line under the date range, on the first screen because nobody scrolls to the foot: "{X} % der Fernzüge in Deutschland sind verspätet. Finde die Verbindung, die trotzdem ankommt →" / "{X}% of long-distance trains in Germany run late. Find the connection that still arrives →", X = 100 minus Germany's long-distance on-time share for the chosen period (live from `/api/leaderboard`, dropped when there is no figure), the whole line linking to the connection search in the page's language, Umami event `lb-cta-search`. The sentence names long-distance trains since later on 2026-09-18 (it first said just "trains", which read as wrong beside the all-trains table's 84.8 % on time); "trains" rather than "stops" is the user's choice, the figure counts stops. The same day a second, louder prompt went under the tables (above the method note and the request box): a full-width brand-red banner whose headline is the same live figure (the homepage slogan "Den Zug buchen, nicht die Verspätung" when there is none), a one-line pitch and a white "Verbindung prüfen →" pill, the whole card one link to the connection search; the hero line's sentence turned brand red for visibility. Both send `lb-cta-search` with `place: hero|banner`. Later on 2026-09-18 the hero's text line was replaced by a quiet version of that banner above the podium (white card, hairline red border, 15 px headline, small red pill, max 760 px; same figure, pitch and button text, `ctaText` gone), and the red banner stays under the tables as `#lb-banner-foot.lb-banner-strong`; `place` is still `hero` for the card and `banner` for the red one. Also 2026-09-18: the podium heading names the metric ("Das Podium · die pünktlichsten Bahnen" / "The podium · most punctual railways"; a longer version with the 6-minute definition was live briefly and shortened) and the map legend is labelled in on-time share like the map's figures, low to high ("Anteil pünktlicher Halte: unter 50 % … 100 %", ramp reversed to dark-to-light), map colours unchanged, after the old "share of delayed stops 0–50 %+" legend read as contradicting the labels.
- **Installable PWA** (2026-08-10, Lighthouse + CDP-verified) — `static/manifest.json` (standalone, DB-red theme, `any` + `maskable` icons 192/512, 180 px apple-touch, an Entschädigung shortcut) and a root-scoped service worker (`static/sw.js`) make the site installable on Android/desktop Chromium and on iOS (every browser there, not just Safari). The worker precaches the app shell (cache-first + background revalidate) and is network-first for `/api/*` with a 10-minute freshness gate, so an offline reload opens the UI but never shows delay data older than ten minutes; the feedback POST, `/stats` analytics, `/health` and cross-origin requests pass through untouched. Lighthouse PWA category scores 1.0 and the worker registers/activates/controls the page. An **in-app install prompt** (2026-08-11, extended to desktop 2026-08-14) makes users aware: on Chromium — Android and desktop alike — a dismissable banner with a real Install button (`beforeinstallprompt`, desktop getting its own lead wording), on iOS the same Install button opens a bottom sheet with the 3-step "Share → Add to Home Screen" instructions — every iOS browser since 2026-08-22, with the lead line named for Chrome or a generic share-menu wording for Firefox/Edge; it self-suppresses when already installed or after a 30-day dismissal. The app icons are the DelayBahn logo's train mark (DB-clock monogram in a train silhouette) in white on the brand-red tile, derived from `logo.png`; brand red is #fd1c17 site-wide since the 2026-08-13 rebrand.
- **Upstream resilience** (2026-07-25, hardened 2026-08-13 and 2026-08-21) — bahn.de session recycling and fingerprint rotation on Akamai 403s, shared-task caches for journeys/locations (journey departures floored to 5-min buckets so "now" searches share keys, TTL 300 s), upstream fan-out capped at 4 concurrent requests, and successful journeys remembered ≤ 1 h — per exact key and per route (from, to, dticket), the route entry serving newer departure buckets of the same travel day, and since 2026-08-13 only within 30 min of the requested departure, so "now" searches survive bucket rollover without a search ever being answered for another time of day — and re-served with a visible age (`staleSeconds`, `Cache-Control: no-store`) when bahn.de refuses; the frontend auto-retries once behind a friendly "DB-Server überlastet" status, and sustained 429 pressure (≥ 8 in 5 min) pushes an ntfy alert (30-min cooldown). Since the 2026-08-13 evening hardening there is **no identity rotation on 429** (the morning's jar swap + profile rotation was removed as rate-limit evasion): a real circuit breaker guards all journey/location calls — 5 failures in a rolling 60 s open it, cooldown 30 s doubling to a 300 s cap with jitter, an upstream `Retry-After` (delta-seconds or HTTP-date) respected up to the cap and opening it immediately, 1 half-open probe closing or reopening it, and a wholesale 403 block force-opening it for 30 s. Failures carry a typed taxonomy (`UpstreamRateLimited/Blocked/Unavailable/ProtocolError`) mapped to honest statuses: upstream throttle/outage with no stale → 503 + `Retry-After`, unusable response → 502, DelayBahn's own per-IP search limit (burst 10/10 s, 40/min via `cf-connecting-ip`) → 429. Coalesced fetches ride `asyncio.shield`, so one waiter's disconnect can't kill the shared task; `/health` exposes circuit state + pipeline counters; all knobs are env vars (`BAHN_*`, `CLIENT_SEARCH_*`) with safe defaults; state is per-process (global on the single-worker deployment). Same-day follow-up after the breaker itself caused an outage (one 429 blacked out every user for the 45 s bahn.de asked for, while the browser's fixed 4.5 s retry landed inside that window and failed for certain): a named `Retry-After` is now used verbatim rather than max'd against the escalation ladder, the frontend waits the advertised cooldown out behind a live countdown and retries itself (75 s budget per search), the optional "falls verpasst" replans — one extra bahn.de call per tight transfer, the biggest multiplier on our upstream rate — are skipped while the circuit is open or the answer is stale and capped at 3 otherwise, and `/api/locations` degrades to an empty suggestion list instead of erroring mid-typing. Since 2026-08-21 the upstream client keeps **one live identity at a time** — one impersonation profile + one cookie jar + one connection, created and discarded as a unit — recycled after a jittered 50–100 requests or 600 s (`BAHN_SESSION_*`), so no single session accumulates enough history to be scored as one continuous heavy client; a 403 burns the whole identity and advances the profile rather than swapping the fingerprint in place on a session Akamai has already flagged, and a 429 still swaps nothing at all. Retired sessions close on a 30 s deferred timer (curl_cffi's `close()` is permanent and would kill in-flight requests), and `session_recycled` joins the `/health` counters. This replaced the old per-profile sessions held for the whole process lifetime, which had let 403 bursts recur overnight on 2026-08-20/21. Since 2026-09-04 every session also sends `Accept-Language: de-DE,de;q=0.9,en;q=0.8` — the impersonation profiles do not, and that day Akamai began answering the search POST without it with `403 OPS_BLOCKED` from every IP (reproduced from ps083 and fresh proxy exits alike); a wholesale 403 that also hits the dev box is a request-shape rule, not IP reputation.
- **Capacity** (2026-07-27) — load-tested via `pipeline/loadtest.py` against a fully stubbed upstream. Sustains 10 sessions/s (~36k sessions/h) at 10.4 % of one core with the event loop effectively unblocked (canary p99 3.9 ms); 30-min soak clean at +11.8 MB RSS. `/health` exposes row count and cache sizes.

Accounts (Firebase Auth, 2026-08-26; since 2026-09-13 the SDK's authDomain is delaybahn.com itself, with `/__/auth/*` proxied to Firebase in `app/main.py`, so a popup-blocked redirect login survives storage-partitioned browsers — the Google OAuth client must list `https://delaybahn.com/__/auth/handler`) carry three things: Delay Geschichten, the post-journey delay reports, and — since 2026-09-05 — **Meine Fahrten** (`/meine-fahrten`, `/en/my-trips`), the journeys an account bookmarked or opened on bahn.de. A bookmark beside every booking button files a journey (a signed-in press on the booking button itself does too; pressed while signed out it opens a short modal explaining Meine Fahrten before the login, and a signed-in press gets the same modal as a receipt with a link to the list — both name the lost-time tally); the page lists them as next/past, shows an upcoming trip's stored search card behind a button (the only control there since the bahn.de link was dropped on 2026-09-05), runs the compensation verdict for a past one in place, hands one of a past trip's trains to the damage report or to Delay Geschichten with the form prefilled (2026-09-05, an arrow per leg in the same panel), and tallies lost time by delay vs cancellation over week/month/year — since 2026-09-11 the tally board is always on the page, showing zeros while there is nothing to add up (signed out, fresh account, only upcoming trips), so a visitor sees what it keeps count of before the first trip; an account with no trips at all gets a single onboarding card in the lists' place — the bookmark pitch ending on that tally, with a search button (2026-09-06). Since 2026-09-11 an unfiled bookmark pulses as a nudge once the visitor holds still on the results for 2 s (the one nearest the viewport middle; re-arms per scroll stop, at most once per bookmark per render, `trip-pulse` Umami event, disabled under `prefers-reduced-motion`). The account name in every header links to it (on phones the home header shows a circled person-icon button in the name's place instead of the old 4em-clipped name, and on desktop the same icon sits at 17px beside the name, 2026-09-06; both are the same link, centered with Abmelden). Since 2026-09-17 that header is the same on every page: leaderboard and stories (and the trips page itself) carry the account corner with the icon and the Meine Fahrten pill like the homepage, under one `header-account` layout class.

## Verified

- File-backed serving (2026-08-07, benchmarked on production hardware over 185 real sampled keys with the live app running): build 37.9 s memory-bounded; init 0.6–0.8 s; cold first-touch queries p50 13.8 / p95 24.4 ms (forced OS-cache drop), warm p50 7–8 ms; RSS ~470 MB. 189/190 keys byte-identical to the in-memory table (the one diff: a tie between duplicate S-Bahn observations that is already nondeterministic between in-memory startups). Corrupt-db fallback exercised: garbage `delays.duckdb` → stderr warning → parquet load serves 19.5 M rows.
- Load test (2026-07-27, `pipeline/loadtest.py` vs the stubbed app on :8001): cold-cache ladder at 2/5/10 sessions/s went from 156-199 % CPU and 100-152 ms event-loop block time to 4.9/9.1/13.6 % and 5.6/5.2/16.1 ms after the sargable-lookup fix; 30-min soak at 10 sessions/s = 185 899 requests, 0 errors, RSS +11.8 MB (0.32 MB/min). Sorted-schema equivalence checked over 400 sampled real (train, eva, time-of-day) triples: 0/754 mismatches once tie-breaks were made deterministic. Cache eviction bounds asserted directly. Not exercised: real bahn.de/IRIS upstreams (deliberately stubbed) and the browser.

- Pipeline: 31-day rebuild (2026-07-26) parsed ~4.1M XML responses → 21.5M merged rows (DE+CH+FR); per-day avg delay 2.3–4.1 min in the original 8-day check (2026-07-12), plausible network-wide.
- Leaderboard (2026-09-05, headless Chrome at 1000 px and 500 px, all three periods, DE and EN): map labels clear of the edges and of each other, the tint ramp visible with transitions disabled, sorting driven over CDP (default → avg delay → stops → stops reversed → period switch with the sort held) re-numbering the badges as expected; `tests/test_leaderboard.py` 5/5 (9/9 since 2026-09-13 with the long-distance split). Long-distance table verified 2026-09-13 in headless Chrome, DE and EN, both tables populated and sorting independently; month view CH 94.8, NL 94.7, FR 78.9, AT 72.6, IT 62.7, DE 49.4 %. Ranking cross-checked against a direct DuckDB query over the same 30 days: CH 98.4, NL 97.2, FR 91.9, AT 87.8, IT 85.3, DE 83.9 % on time. Phone table re-verified 2026-09-05 in Chrome touch emulation at 390 px (DPR 2, live API proxied in): no page overflow, sticky rank+country pair holds while the wrap scrolls 699 px of table, taps on the cancellations and stops headers sort and re-number.
- API: `curl /api/journeys` Berlin Hbf → München Hbf returns journeys with `delayStats` at 7/7 days matched on all ICE legs.
- Cross-check: same train numbers (e.g. ICE 1505) found in both bahn.de journey results and the historical table at the arrival EVA — validates EVA padding + train-number matching.
- Browser (Playwright, headless Chromium): autocomplete → search → result cards with badges → "Wenigste Verspätung" sort reorders correctly → booking deep-link contains correct stations and departure time. No console errors.
- Past mode (2026-07-23, curl + Playwright): bahn.de journey search returns past dates ≥ 60 days back (no prices); exact per-day delays match parquet rows (ICE 1507 @ München 16.07. = +52); compensation tiers correct (+47→0 %, +62/+65→25 %, +126→50 %); simulation cases: missed 9-min transfer in Nürnberg → replacement found, +42 → no claim; cancelled ICE 2512 → RE2+RB31, +238 → 50 %; Paris Est→Tübingen with cancelled IC 2167 → +120 → 50 %. Out-of-coverage dates get a friendly error; DE/EN complete; future mode regression-clean.
- Live same-day lookups (2026-07-25, curl + stubbed-IRIS unit checks): 17:00 Hamburg→Berlin searched at 22:15 the same evening returned ICE 2547 +34, cancelled FLX 1247 → re-planned to +58, ICE 607 +22, FLX 1349 +5, ICE 873 +17; journeys later that night correctly `pending` with no verdict; cold search 1.36 s / 13 IRIS calls for a 6-result page, warm 0.00 s. Cross-checked against bahn.de `echtzeit` on eight just-run legs: six exact, two off by 1 min (IRIS matches what the parquet will store). `fchg` lookback measured at 14.2–16.7 h across Hamburg/München/Köln. With no credentials the live path stays off and past mode is byte-identical to before.
- S-Bahn matching (2026-08-02, curl vs live bahn.de + DuckDB probes): S5 @ Berlin Ostkreuz 0 → 7/7 days matched; Berlin Hbf→Ostkreuz future mode all S-Bahn legs carry medians, past mode exact delays (S3 +5 on 07-30); CH regression diff byte-identical (S7/S4 at the three busiest CH stations, old code vs new on the same parquet); ICE medians unchanged; Kassel→Bad Wildungen bus legs get no `delayStats` key. Live same-day line matching not exercised (no IRIS credentials locally) — same key logic as the parquet path; glance at it on ps083 after deploy.
- Language URLs (2026-08-15, curl + headless Chrome vs the worktree app on :8099): all 15 URLs return the expected status/redirect target (`/en`→`/en/`, `/en/compensation/`→`/en/compensation`, `/de`, `/de/`, `/index.html`→`/`); JSON-LD re-parses as valid JSON on all four pages after the rewrites; canonical + the de/en/x-default hreflang triple correct and reciprocal on each; 64/64 `data-i18n` elements translated on `/en/` (only `D-Ticket` matches German, being identical) with `&` escaped; `<h1>` + `body.past-mode` survive on both compensation pages; screenshots of `/`, `/en/`, `/en/compensation` render with unchanged pill geometry; pytest 58 passed. Not exercised: a real click-through of the toggle in a browser (the pills are plain links with verified hrefs and no `preventDefault`), and the localStorage redirect for returning English users.
- Past sub-page (2026-08-03, curl + Playwright, 30/31 checks): served /entschaedigung HTML carries past title/description/canonical/og + h1, trailing slash 301s, sitemap lists both URLs; past mode initializes from the path (banner, hidden chart/CTA/window field, coverage-clamped dates); DE/EN toggle swaps the past title; legacy `?mode=past` redirect restores a full search, restored date beats the clamp; homepage regression-clean; end-to-end past search returned cards with cancelled/missed legs, continuations and a 25 % claim. Only failure: Umami CORS from the localhost origin (test artifact). Unverified: refund-cta/nav Umami events surviving the real navigation — check the dashboard after deploy.
- Feedback + contact (2026-08-10, headless Chromium vs the worktree server on live bahn.de): the ask visible under single-search, round-trip outbound/return/summary and compensation-check results; a vote on the outbound leg still yields a fresh ask on the return list; a repeat search re-arms; vote/follow-up/thanks states walk correctly and both POSTs landed as merged rows in feedback.db; contact dialog opens on / and /entschaedigung, EN toggle switches its strings, send closes it and the assembled address is kontakt@delaybahn.com; no console errors beyond the dev box's /stats 502.
- Overload UX + breaker correction (2026-08-13, 57-test suite + real Chrome via Playwright against a scripted 503-then-200 backend): a 45 s `Retry-After` stays 45 s across repeated opens instead of climbing to the 300 s cap; the if-missed enrichment fires and stays capped at 3 when healthy, makes zero calls when straining or stale; autocomplete degrades to `[]`. Browser: the countdown appears un-error-styled and ticks down, results arrive with no user action, status clears, EN toggle swaps the countdown live, a superseding search leaves one result set, no page exceptions; at bahn.de's real cadence (45 s, refusing forever) it waits once, attempts exactly twice, then shows the friendly message.
- D-Ticket modes (2026-08-21, 78-test suite + headless Chromium against the worktree app on live bahn.de): München → Augsburg returns 5 paid ICEs in "off", the same ICEs interleaved with free RB86/RE9 rows in "all", and only regional rows in "only"; Berlin → Leipzig's ICE+S-Bahn journey reprices 55,99 → 49,99 € with `pricePartial`; mutual exclusion holds when each switch is clicked in turn; `?dticket=` restores for `all`, `only` and legacy `1`; "Günstigster Preis" puts the covered rows first; a both-covered round trip totals "D-Ticket" and a mixed one 12,99 € with the partial tooltip; the bahn.de hand-off sends `dltv` alone for "all" and `dlt`+`dltv` for "only"; the search card lays out correctly at 1280/1000/900/800/750/700/390/360/320 px in both languages, on the compensation page and with the round-trip fields open.
- Return-leg preflight (2026-08-13, 58-test suite + real Chrome via Playwright against the worktree app on live bahn.de): a 17:00 answer no longer serves a 22:00 query while the 30-min bucket-rollover case still does; the happy path issues exactly two `/api/journeys` calls (outbound, then the return at the requested time) and clicking "Weiter" issues none, with the return list starting inside the requested window; with the return leg 503ing twice the outbound list stays unrendered (0 "Weiter" buttons) behind the return-specific countdown, then appears once the return recovers.
- Resilience hardening (2026-08-13, `uv run --group dev pytest` — the repo's first test suite, 48 tests, upstream fully mocked): cache miss/hit/expiry/5-min bucketing, stale served under 429 and while the circuit is open but not past the stale TTL, 30 concurrent identical searches = 1 upstream call, a cancelled waiter leaves the shared fetch alive, `Retry-After` parsed in both forms and respected, no retry loops (1 call per plain 429, ≤ 2 with a short Retry-After), breaker threshold/half-open/floor/cap transitions, limiter burst + sustained + per-client isolation, 5xx/timeout/malformed taxonomy, 503/502/429 mapping. Live smoke on the worktree app: 12 rapid identical searches → exactly 1 bahn.de request, 10×200 then 2× our 429 with `Retry-After: 8`; `/health` counters matched (searches 10, misses 1, hits 9, client_rate_limited 3).
- Surge degradation (2026-08-13, unit + real-ASGI-app + headless Chrome 146 CDP vs a scripted backend): stale journeys served with their age and `no-store` while fresh answers stay cacheable, unseen routes still 502; browser flows "502 then recover" (retry status, exact DE/EN notice strings, language toggle live-swaps the notice) and "hard fail" (friendly error-styled message, notice hidden) both pass with exactly one retry and no console exceptions.
- PWA (2026-08-10, worktree server on :8099 over live 19.5 M-row data): `npx lighthouse@11.7.1 --only-categories=pwa` = 1.0 — installable-manifest, maskable-icon, themed-omnibox, splash-screen, viewport and content-width all pass. A CDP check in headless Chrome 146 (`--remote-allow-origins=*`) confirmed the service worker registers at scope `/`, reaches `activated`, and controls the page after a reload (`controller.scriptURL = /sw.js`) — Lighthouse 11 no longer has a standalone service-worker audit, so registration was proven directly. All 8 precache URLs return 200; sw.js + the inline registration pass `node --check`. The `/entschaedigung` rewrite, its 301, and `/api/coverage`/`/api/locations` regression-clean.
- PWA install prompt (2026-08-11, headless Chrome 146 CDP, worktree :8099): 19/19 — desktop suppressed (banner hidden after a synthetic `beforeinstallprompt`, `userAgentData.mobile` false), Android shows the banner + Install button (mobile UA metadata) with a working accept and EN toggle, iOS Safari shows the Share hint with dismissal persisting across reload; no console exceptions. The logo-based icons render solid white on red at 192/512/apple-touch (any + maskable).
- iOS install sheet (2026-08-11, headless Chrome 146 CDP): 20/20 — iOS now shows the same Install button, which opens a bottom sheet with exactly 3 steps (✕ / backdrop / "Got it" all close it); Android native install and desktop suppression unchanged; no exceptions. Sheet screenshot-checked at 390 px (DE).
- Desktop install prompt (2026-08-14, headless Chrome 146 CDP, worktree :8099): the banner now appears on a desktop UA after a synthetic `beforeinstallprompt` — Install button visible, lead line "Öffne delaybahn.com im Browser deines Handys und tippe auf „Installieren“.", `dataset.i18n = installLeadDesktop` so the EN toggle swaps it live, `preventDefault()` called on the cancelable event, `track("install", {step:"shown", platform:"desktop"})`, and clicking Install runs `prompt()` → accepted → banner hidden. Android (mobile UA metadata, `platform:"android"`, home-screen wording) and iOS Safari (Install → 3-step bottom sheet) unchanged; no console exceptions in any of the three. Screenshot-checked at 1280 px (DE).
- Chrome-on-iOS install steps (2026-08-22, on-device): banner and sheet confirmed on a real iPhone in Chrome — Install button, "In Chrome, in 3 steps:" lead, EN translation applied. The install itself did not complete on Safari's wording, which is what the Chrome-specific steps address; that retest is pending.
- iOS install banner on non-Safari browsers (2026-08-22, static checks only): the Safari-only gate (`isIOSSafari`) is gone, so all four iOS UA strings (Safari, CriOS, FxiOS, EdgiOS) now take the sheet path with the right `iosBrowser`, while Android and desktop Chrome still route to `beforeinstallprompt`; `node --check` clean on app.js and sw.js; `_en_strings()` picks up `iosSheetLeadChrome`/`iosSheetLeadOther`. Device verification on a real iPhone is pending — in particular whether Chrome-iOS's installed web app reports `display-mode: standalone`, which is what suppresses the banner inside it.
- Snippet fix invisibility (2026-08-17, two servers + headless Chrome): the brand-leading descriptions and the `data-nosnippet` disclaimer change nothing a visitor sees — with `--force-prefers-reduced-motion` freezing the train scene (the page is otherwise nondeterministic frame to frame, proven by a same-server double-shot), screenshots of `/` and `/en/` are byte-identical to main; the post-JS DOM diff of `/` shows exactly two changes, the head tag and the span wrapper whose visible characters match. All four variants carry the intended description; pytest 58 passed.
- Social follow links (2026-08-17, 37 checks in headless Chrome over CDP vs the worktree app on :8077 — no Playwright in this environment, so the driver is a small hand-rolled CDP script): all three `<svg>` survive `_translate()` on `/en/` and `/en/compensation` and `applyLang()` client-side, which is the regression the whole design guards against; the row is on the very first ask with child order `lead > 👍 > 👎 > follow > ✕`, on the votes' line and left of the dismiss, surviving both the vote and the submit, and a second search re-arms without duplicating it; the footer row is the footer's first element and renders above the GitHub link, unlabelled, on one line at 360 px with no horizontal overflow (the ask wraps rather than overflows); clicks from both rows emit their events; JSON-LD re-parses on every variant. Three initial failures were bad assertions, not defects — `inline-flex` is blockified to `flex` on a flex item, an `applyLang("en")` leaked through localStorage into a later German assertion, and `align-items: center` gives an 11 px label and 20 px icons different `top` values with no wrapping involved.
- Delay reasons (2026-07-26, curl + Playwright): forced 31-day rebuild with the reason-aware parser → 3.7M of 21.5M rows carry a cause, 79.3 % of DE arrivals delayed ≥ 10 min; all observed codes (1–69, 99) covered by the frontend map. API: future-mode per-day reasons (ICE 801 07-24 +122 → 47) and past-mode exact-day reasons (cancelled ICE 723 → 36); live same-day at 13:10 for an 08:00 Hamburg→Berlin (FLX 1237 +18 → 45, not-yet-run leg stays pending). Browser: 6/6 day charts showed cause tooltips, past-mode badge titles carry causes, EN switch translates them; no console errors.
- Delay Stories banner dismiss cross (2026-08-27, headless Chrome over CDP at 390×844 and 1200 px): the ✕ sits at the row's right edge like the install prompt's, one click hides the row, it stays hidden across a reload, a timestamp back-dated 61 days brings it back, and the install prompt is unaffected; title and aria-label read "Hinweis ausblenden". Server-rendered `/`, `/en/` and `/entschaedigung` all carry the new markup, `/en/` with `/stories` on the link — the `_page_html()` regex had to move from the old `<a id="stories-banner">` to the link's class.

- Meine Fahrten (2026-09-05, 287 tests + a TestClient script over the whole API + Playwright against the real local server at 1100/390 px): the login round trip and the parked bookmark completing on return, presses posting `add` and the DELETE undoing them, idempotence per (account, itinerary), the 200-cap eviction, foreign-`uid` 404s, 409 on a check for an upcoming trip, verdicts stored once the parquet covers the day and served back off the search budget, the delays-vs-cancellations split, and the `ALTER TABLE` upgrade of an existing trips.db. A cancelled ICE was really re-planned through bahn.de during the run, so the simulation path is exercised end to end.

## Key implementation decisions

- **bahn.de web API instead of v6.db.transport.rest**: transport.rest (planned upstream) was down (503 on v5+v6) during development. bahn.de's own web API is what db-vendo wraps anyway; it returns Berlin-local naive times (no tz conversion) and ticket prices. See log.md 2026-07-12.
- **Arrival delay, not the dataset's `delay_in_min`**: the dataset's column prefers departure delay; passengers care about arrival. We compute `date_diff('minute', arrival_planned_time, arrival_change_time)` ourselves.
- **Delay score = final leg's median arrival delay** (delay at the destination), with worst-leg median as tiebreaker/transfer-risk signal (avg → median switched 2026-07-19).
- **The leaderboard ranks by on-time share, not median or mean delay**: the median arrival delay is 0–1 min in all six countries and cannot separate them; the mean is set by the few percent of trains 30+ min late, so one disrupted day moves it more than a week of ordinary running. The share is also the figure every operator publishes; the mean only breaks ties. If a minutes figure is ever added, the 90th percentile ("9 of 10 trains within X min") is the candidate.
- **Tight-transfer warning**: `tight_transfers()` in app/main.py flags transfers where the arriving leg's median delay leaves ≤ 2 min of buffer (`TRANSFER_TOLERANCE_MIN`); walking legs between trains are subtracted from the planned gap. Entries carry an `unlikely` flag (median delay > transfer time + 30 min, `UNLIKELY_EXCESS_MIN`) that switches the warning to "⛔ Unwahrscheinlicher Umstieg / Unlikely transfer", overrides the journey's headline badge with a red "⛔ Anschlussrisiko / Connection risk" pill (final-leg stats are meaningless if an earlier connection is missed), and demotes the journey in the least-delay sort (normal → unlikely → no data). Since 2026-07-26 a non-empty `tightTransfers` also puts a yellow "⚠ Knapper Umstieg / Tight transfer" pill in the journey header (tooltip lists the stations); it renders only when no transfer is unlikely, since the red pill replaces the median badge entirely. Next to the pill the median delay badge shows only when red (median ≥ 10 min); green/yellow/gray are suppressed, which also hides that card's badge-click day chart (2026-07-27). The header's badges, price, and booking button sit in a `.journey-cta` flex group so a too-narrow card wraps them together as a right-aligned block instead of orphaning the last item, and `.badge` carries an explicit line-height so emoji glyphs don't inflate pill height (2026-07-26). Below 700 px the header switches to a `1fr auto` grid with `.journey-cta { display: contents }` — times/price top row, badges right, full-width CTA bottom row — written against `.journey-head .price`/`.badge` so past mode (no `.journey-cta` wrapper) gets the same layout (2026-08-02).
- **EVA normalization**: dataset EVAs are 8-char zero-padded strings; bahn.de extIds are unpadded → `pad_eva()` in app/delays.py.
- **Window ends yesterday** by default: today's raw uploads (every 6 h) are incomplete.
- **Selectable averaging window (7/15/30 days)**: pipeline default is `--days 31` (30 full days, raw mirror ~4.5 GB at ~140 MB/day); `/api/journeys` takes `window` (Literal 7/15/30, default 7); `leg_delay_stats` filters `CAST(arrival_planned_time AS DATE) >= _max_day - (window-1)` — anchored to the newest day in the parquet, not now(), so stale data still yields full windows; cache keyed by `(train, eva, window)`. If the parquet holds fewer days than requested, results degrade gracefully (badge shows e.g. 7/30 Tage).
- **Site-wide DE/EN toggle (frontend only)**: header pills replace the chart-only toggle. Static text carries `data-i18n`/`data-i18n-placeholder`/`data-i18n-title` attributes; dynamic strings (status, badges, journey cards, tooltips) go through the `I18N` dict + `t()` in static/app.js. Status messages are stored as key+params so they re-render on switch; `<html lang>`, `document.title`, and the chart SVG (de/en variant) follow. Choice persists in localStorage. Data values (station names, prices, times) stay as the API returns them.
- **Automated daily refresh**: systemd timer `delaybahn-pipeline.timer` on ps083 runs the pipeline daily at 05:30 Europe/Berlin (`Persistent=true`, up to 15 min randomized delay), then restarts `delaybahn.service` to reload the parquet. `prune_old_raw_days()` in pipeline/build_delay_db.py removes raw-mirror days outside the window after each build, keeping `data/raw_data/` a rolling ~31 days.
- **Past-journey compensation checker (2026-07-23)**: `/api/journeys?mode=past` reuses the normal bahn.de search (works for past dates) and attaches per-leg `delayOnDate` (exact-day variant of the median lookup; `leg_delay_on_date`/`leg_departure_on_date` in app/delays.py). `_simulate_walk()` rides the journey with actual delays — a transfer is made only if the connecting train's actual departure (own delay included) leaves > 2 min — and on a miss/cancellation re-plans via bahn.de from that station to the destination (`_next_connection()` probes ready−45 min and picks the earliest actual arrival, so the just-ridden delayed train continuing onward is considered; ≤ 3 chained re-plans). Compensation (25 % ≥ 60 min / 50 % ≥ 120 min vs booked planned arrival) comes from the simulated arrival; the claim button opens a claim-steps modal (2026-07-31) whose continue link goes to bahn.de/buchung/reiseuebersicht/vergangene (account-based digital claim; Fahrgastrechte form fallback inside the modal). Frontend strikes out missed legs and renders the actually-taken continuation. Since 2026-08-14 replacements exclude FlixTrain (`_flix()` matches the BEF/Beförderer `zugattribut` now carried on `leg["line"]["operator"]`, `FLX` line-name prefix as fallback; whole itinerary dropped if any leg is Flix) — Flix runs its own tariff, so a stranded DB passenger cannot board one, and because it often arrives earliest it was displacing the real next connection and wiping out claims (visitor report).
- **Browser-fingerprint rotation + upstream cache (2026-07-25)**: Akamai blocks one curl_cffi `impersonate` profile at a time (chrome went 403 OPS_BLOCKED on 2026-07-23), so `app/bahn_api.py` holds a session per profile (`firefox135`, `safari17_0`, `chrome`) and rotates on a 403 before retrying. Locations (600 s) and journeys (300 s since 2026-08-13; was 120 s) are cached as shared asyncio tasks in a 512-entry LRU, so concurrent identical searches ride one upstream call (awaited through `asyncio.shield` since 2026-08-13, so one waiter's disconnect can't cancel the shared fetch); failed tasks are evicted so a block never sticks. Akamai also rate-limits per session cookie (429s during the 2026-08-13 surge, per-IP probes unaffected): journey departure times are floored to 5-min buckets before cache key + upstream body, and `_upstream_sem` bounds fan-out at 4 concurrent upstream requests. The morning's 429-triggered jar swap + profile rotation lasted one day — removed the same evening as rate-limit evasion; 429s feed the circuit breaker instead (below).
- **Live same-day delays from the DB Timetables API (2026-07-25)**: the batch design can only answer a journey the next morning (~06:20: HF publishes raw IRIS every 6 h, the build window ends yesterday, the timer fires 05:30). `app/live_delays.py` closes that by reading the delay at request time from IRIS `plan` (train number + planned time → stop id) + `fchg` (`ar/@ct`) — deliberately the *same field* the nightly build stores, so the number seen minutes after arrival is what the parquet will hold, and users never see it change. It is consulted only for days past `delays.coverage()[1]`, always with a parquet fallback, and an upstream failure degrades to "pending" instead of failing the search. A stop still in the future returns unknown rather than on time, since IRIS's silence is only punctuality once the stop is behind us. `warm()` prefetches an itinerary's stations concurrently so the per-leg lookups stay sync and `normalize_leg` stays unchanged in shape. Measured lookback 14–17 h, which overlaps the 06:20 build — so no intraday rebuild is needed. Credentials optional: without them the path is inert.
- **Local station autocomplete (2026-07-25)**: `/api/locations` answers from an in-memory index built from the parquet at startup (folded names collapse umlaut/Hbf spellings and multi-level EVAs; ranked prefix > word-start > substring, Hbf first, then observation volume), falling back to bahn.de only for stations with no delay history. Cuts the common keystroke path off the bot-protected upstream entirely.
- **Multi-country delay data (2026-07-20, AT 2026-08-14, NL+IT 2026-08-15)**: per-country producers emit the DE 17-column schema into `data/{at,nl,ch,fr,it}/days/*.parquet`; `merge_delays.py` unions them with a partition on the padded-eva country prefix (080/081/083/084/085/087) and a global last-midnight cut on both `time` and `arrival_planned_time` so no source shifts `_max_day`. CH: istdaten v2 (BPUIC == bahn.de extId; S-Bahn keyed by line digits because bahn.de sends the line, not the run number, for Swiss S-Bahn only). FR: SNCF GTFS-RT poller → `data/fr/obs.sqlite` (keep-last upsert) → daily consolidation; station IDs crosswalked SNCF-UIC → DB-EVA via committed `config/fr_uic_to_eva.json`; cancellations = trip CANCELED / stop SKIPPED, bare trip-cancellation markers propagated at consolidation. AT: ÖBB HAFAS (Scotty) ARR+DEP board poller over the ~200 busiest stations (`config/at_poll_stations.json`, ranked by ÖBB-GTFS rail stop events, ids resolved via bahn.de orte + board probing) → `data/at/obs.sqlite` → daily consolidation; the earlier "no per-stop open data" deferral was re-checked 2026-08-14 — ÖBB's new MMTIS CSVs are journey endpoints only, so board polling stayed the path. See log.md 2026-08-14. NL: OVapi train-only GTFS-RT poller, whole network in one 120 s fetch; train/station identity from OVapi's proto extension (`IFF:<type>:<number>` + IFF station code), so the daily rotation of OVapi's internal static-GTFS ids never touches the store; station codes crosswalked via committed `config/nl_stations.json` (Rijden de Treinen stations CSV; NL UIC codes are DB EVA numbers) → `data/nl/obs.sqlite` → daily consolidation; history seeded per-month from the Rijden de Treinen train archive (CC BY 4.0, credited in the footer). See log.md 2026-08-15. IT: ViaggiaTreno run tracking — 254 hub boards discover runs (both board types carry the andamentoTreno key triple), then each run is polled via `andamentoTreno`, which returns every stop's planned/actual times and cancellation flags in one call; station IDs crosswalked RFI-code → DB-EVA via committed `config/it_scode_to_eva.json` (trainline-eu seed + bahn.de orte, DB EVAs differ from Italian UIC like for FR). See log.md 2026-08-15.
- **German S-Bahn keyed by line label; untracked products gated (2026-08-02)**: the delays-table `train_no` is a CASE — DE `train_type='S'` rows (`eva LIKE '080%'`) key by `line_number` ("S5", bare digits get an "S" prefix) because bahn.de sends the line label as fahrtNr for German S-Bahn; all other rows keep `ltrim(train_number,'0')`. CH is fenced by EVA prefix (Swiss S-Bahn nummer is bare digits, already re-keyed in build_ch_days.py). Live IRIS path mirrors this: the plan parser keeps `ar/dp l`, and `_lookup` accepts a line match only for non-numeric query keys. `UNTRACKED_PRODUCTS` (BUS/TRAM/UBAHN/SCHIFF/ANRUFPFLICHTIG, blocklist so unknown products fail open) skips the lookup at all four consumption points and drives the frontend's "nicht erfasst" badge — also closes the latent false match of e.g. tram line "1" against train number 1. S-Bahn badge semantics: nearest run of the line within the ±120-min tolerance per day, the intended proxy for a headway service.
- **Pipeline deployed + day-incremental (2026-07-26)**: the 4-step `delaybahn-pipeline.service` (DE→CH→FR→merge, then app restart) and `delaybahn-fr-poller.service` are installed and enabled; the leftover session poller from 07-20 was killed. `build_delay_db.py` now parses raw XML once per day into a persistent `data/de/parsed/` cache and re-parses only days with new raw files, cutting the nightly run from ~40 min to minutes. `merge_delays.py` gained a symmetric lower window cut (`--window-days`, default 30) so the served parquet holds only days the app can serve and `/api/coverage` no longer advertises FR/CH-only days that are empty for DE journeys. See log.md 2026-07-26.
- **DE build staged so one operator spills at a time (2026-08-26; caps raised 2026-08-29)**: `merge_parsed()` in `build_delay_db.py` writes the three dedups (`plan`, `fchg`, latest `reason_code` per id) to their own parquets under `data/de/.stage/` before the join, under `memory_limit='4GB'`, `threads=4` (since the CX33 rescale on 2026-08-29; `2GB` / `2` on the 4 GB / 38 GB CX23 the staging was written for) and `preserve_insertion_order=false`, so DuckDB spills one operator at a time — the single dedup+join query had held all three intermediates plus the join at once and ran the disk (6.4 GB free at rest) out with `No space left on device`. The dedups are `row_number() OVER (PARTITION BY id ORDER BY xml_timestamp DESC) = 1` windows, not `DISTINCT ON`: DuckDB plans the latter as a hash aggregate with one `arg_min` per column plus a full `ORDER BY`, which OOMed at the cap on the box (`failed to pin block`), while the window spills cleanly and picks the same rows. No `ORDER BY time` on the output any more — nothing reads `data/de/delays.parquet` in order (merge unions it, `build_db_file` sorts), so the file is ~730 MB unsorted instead of ~550 MB; measured end to end on the 31-day cache: 38 s, 1.5 GB spill + 0.9 GB stage, 2.35 GB RSS. See log.md 2026-08-26.
- **Per-country coverage alert after every merge (2026-08-26)**: `check_coverage()` in `merge_delays.py` compares each merged source's newest `arrival_planned_time` day with yesterday (Berlin — the window ends at last midnight and every source delivers the previous day by the time the timer runs) and, when any source lags or has no rows, prints the list to the journal and pushes one high-priority ntfy message (`delaybahn pipeline: DE coverage lagging`, one line per source with its lag, the day the app serves, and the journalctl command). The unit's `ExecStart=-` on the country steps is what let the DE build fail for a week (2026-08-19..25) with `/health` green; the `OnFailure` hook only fires when the whole unit fails, so this is the alert for the silent case. The topic comes from `NTFY_TOPIC` in the environment or, since the pipeline unit passes none, from `.env` — that one key only, read the way `app/config.py` reads it, so the merge's own network path is untouched. Never raises: no topic or an unreachable ntfy is a stderr warning. See log.md 2026-08-26.
- **If-missed alternative behind tight-transfer warnings (2026-08-03)**: `_if_missed_connection()` in app/main.py fills `tt["ifMissed"]` for every tight transfer in future mode — the next realistic connection from the transfer station to the journey's destination, assuming the passenger is ready at planned arrival + the arriving leg's median delay (+ walk minutes); catchable = planned departure leaves > `TRANSFER_TOLERANCE_MIN` after that (excludes the missed train by definition), earliest planned arrival wins. Reuses the past-mode `_replan` helper via one `asyncio.gather` after journey assembly (one deduped bahn.de call per tight transfer); `tight_transfers()` entries now carry `depLegIndex`. Frontend renders it as a disclosure button on the warning strip; open panels cross out the legs from the missed departure onward (earliest open miss wins) via the past-mode `.leg-missed` style. Skips FlixTrain candidates since 2026-08-14, same rule and same reason as the past-mode replan. Since 2026-09-02 the replan carries the search's own restrictions — `dticket` ("only" restricts, "all" folds onto "off" since that passenger accepts paid trains) and the narrowed `products` set — forwarded to `bahn_api.journeys()` and keyed into `_replan_cache`/`_if_missed_cache`, so a "Nur D-Ticket" search no longer gets an ICE as its fallback (visitor report; a D-Ticket holder cannot board it). Past-mode replans stay unrestricted by design.
- **Risk sort ranks within tiers by `minTransferMargin` (2026-07-26)**: min(transfer time − arriving leg's median delay) over *all* train-to-train transfers, computed server-side with the same `_transfer_pairs` helper as the tight-transfer detection; the risk sort orders each pill tier by that slack descending (direct journeys first, no-data journeys last). See log.md 2026-07-26.

- **Echtzeit passed only where it deviates (2026-08-03)**: `normalize_leg` sets `departure`/`arrival` (and `journeys` sets `ezDurationSeconds`) only when the live value differs from the planned one, so their absence keeps meaning "on schedule" and the frontend needs no on-time special case. `_transfer_pairs` prefers live times over the schedule for the gap; past mode ignores the live fields entirely.
- **Past mode on its own sub-page, served by rewriting index.html (2026-08-03)**: `/entschaedigung` is a FastAPI route that regex-rewrites `static/index.html` per request (`_past_page_html()`) — head tags, `body.past-mode`, banner `<strong>`→`<h1>` — instead of a second HTML file, so homepage edits can never leave the sub-page stale. The path decides the frontend mode; `setMode()` is gone and mode-entry links are real `<a>`s, which makes the page crawlable and drops the `?mode=past` state from URLs (legacy links redirect client-side).

- **Stale fallback + overload UX (2026-08-13)**: `bahn_api.journeys` returns `(data, stale_age_seconds)` — successful fetches are remembered (256-entry LRU, ≤ 1 h, stamped at fetch completion so the age is honest) and served with their age when bahn.de refuses; `/api/journeys` marks them `staleSeconds` and `Cache-Control: no-store` so no cache pins a degraded copy. The frontend auto-retries a 429/502/503/504 exactly once after 4.5 s behind a non-error "überlastet" status (search + paging), fails to a friendly bilingual message, and renders stale results behind an amber notice strip that re-renders on language switch. Upstream 429s feed a rolling 5-min counter; ≥ 8 fires one ntfy alert per 30 min (same NTFY_TOPIC/NTFY_URL as feedback).
- **Wait the cooldown out client-side rather than failing (2026-08-13)**: the server knows exactly how long bahn.de wants us to pause and already sends it as `Retry-After` on the 503, so the browser honours that number — a live countdown in the status line, then an automatic retry, up to 75 s per search (`RETRY_MAX_TOTAL_S`, 60 s per single wait, 8 s when the header is missing). A `searchGen` counter supersedes a countdown the user walked away from, so the abandoned fetch renders nothing and does not re-enable the newer search's button. Rationale: bahn.de's windows are ~45 s and users tolerate that far better than "try again later"; the previous fixed 4.5 s retry was inside the cooldown by construction and could only fail. The breaker's own escalation was corrected at the same time — a named `Retry-After` is authoritative and used verbatim (capped), never max'd against the doubling ladder, which would otherwise have turned a 45 s ask into a 300 s blackout after a few opens. Since 2026-08-17 only the first countdown uses the plain "überlastet" wording; every later round switches to a numbered variant ("DB-Server weiterhin überlastet – 2. Versuch in …" / "DB's servers are still busy – retry 2 in …"), so a second countdown no longer reads as the first one looping.
- **Optional enrichment yields to the searches users wait on (2026-08-13)**: `_if_missed_connection` costs one extra bahn.de call per tight transfer per search — the largest multiplier on our upstream rate — for a disclosure most readers never open. It is skipped when `bahn_api.healthy()` is false or the answer is already stale, and capped at `BAHN_MAX_IF_MISSED_REPLANS` (3); the tight-transfer warning itself is unaffected, and the frontend already treated a missing `ifMissed` as "nothing to disclose". `/api/locations` follows the same principle in reverse: a failed suggestion lookup returns `[]` rather than an error, since the local station index already answers everything with delay data.
- **Circuit breaker + per-IP limits instead of identity rotation (2026-08-13)**: bahn.de failures are a typed taxonomy (`UpstreamRateLimited/Blocked/Unavailable/ProtocolError`) raised by `bahn_api._request`, which runs every call through a `CircuitBreaker` (threshold 5/60 s; cooldown 30 s → 300 s cap, doubling + jitter; `Retry-After` in delta or HTTP-date form respected up to the cap and opening immediately; 1 half-open probe; 403-wholesale force-opens 30 s). The only 429 retry is one same-session wait when `Retry-After` ≤ 2 s — never a new jar or profile, which would be rate-limit evasion. `app/main.py` maps the taxonomy to 503 + `Retry-After` (throttle/outage, after the stale fallback), 502 (unusable response) and keeps our own 429 for clients over the per-IP search budget (`app/ratelimit.py`: burst 10/10 s + 40/min sliding windows, IP from `cf-connecting-ip` — trustworthy since the origin is only reachable through the Cloudflare tunnel — rejected attempts uncounted so limited clients recover). Counters + circuit state ride `/health`; knobs are env vars via `app/config.py` (`BAHN_*`, `CLIENT_SEARCH_*`); everything is per-process, matching the single-worker deployment.
- **A round trip is only shown once both halves exist (2026-08-13)**: step 2 used to run after the user had picked an outbound, so a throttled bahn.de surfaced as a dead end two steps in — or, until the same day's skew bound, as connections for an entirely different time of day. `runSearch()` now calls `preflightReturn()` before rendering the outbound list; `fetchJourneys(pagingRef, opts)` takes `{leg, maxTotal, retryStatusKey, retryAgainStatusKey}` so that probe can fetch the reverse direction with a longer budget (150 s vs 75 s — abandoning it wastes an outbound answer already paid for) and name which half is missing. The proven payload rides `state.returnPrefetch` into step 2 and is reused when its departure string still matches, so the guarantee costs one upstream call, not two; it is dropped on a new search and on any window/D-Ticket refetch. A probe that legitimately returns zero connections counts as answerable — retrying there would never terminate.
- **PWA offline strategy (2026-08-10)**: `static/sw.js` precaches the app shell and serves it cache-first (background revalidate), but every `/api/*` response is network-first and stamped with an `sw-cached-at` header; the offline fallback serves a cached delay response only if it is younger than 10 min (`API_MAX_AGE`), else 503 — the app must never show stale delay data. The precache list pins the current `?v=` cache-busters, so a buster bump must bump `SHELL_VERSION` and the pinned URLs together to drop old shells. Non-GET (the feedback POST), `/stats`, `/health` and redirects are deliberately not cached. `sw.js` has a fixed URL that can't be `?v=`-busted, so an explicit `/sw.js` route (app/main.py, 2026-08-11) serves it with `Cache-Control: no-cache` — otherwise Cloudflare edge-caches it (~4 h) and a new `SHELL_VERSION` can't roll out. The HTML documents have the same unbustable-URL problem, and since 2026-08-15 the mount is a `HtmlNoCacheStatic` subclass that stamps `Cache-Control: no-cache` on any `text/html` response (`/entschaedigung` sets it on its own `HTMLResponse`): without it `StaticFiles` sent only a `Last-Modified`, and browsers' heuristic freshness (~10 % of the document's age) served a weeks-old homepage — which is how a user hit a page still missing the 2026-07-28 footer disclaimer. `no-cache` forces revalidation, not re-download; `Last-Modified` still yields 304s, and the versioned assets are untouched.
- **Walk legs name their duration (2026-08-14)**: walk rows render "Fußweg · 7 min · A → B" from the leg's own `plannedDeparture`/`plannedArrival`, which the payload always carried and the renderer used to drop. It exists to explain the tight-transfer figure: `_transfer_pairs` subtracts the walk from the arrival→departure gap, so a 23:25 arrival and a 23:32 departure legitimately warn "0 min", and until now nothing on the card accounted for the missing seven minutes. The transfer sentence itself was deliberately left alone. `node --check` only — not browser-verified.
- **Install prompt on every installable browser (2026-08-11, desktop added 2026-08-14)**: `beforeinstallprompt` drives an in-page Install button on Android *and* desktop Chromium; `navigator.userAgentData.mobile` (UA `/android|mobile/` fallback) no longer gates the banner, only the lead wording — "vom Startbildschirm" is a phone sentence, so desktop points at the phone instead: "Öffne delaybahn.com im Browser deines Handys und tippe auf „Installieren“." (`installLeadDesktop`, swapped via `dataset.i18n` so a DE/EN toggle keeps it) — and the `track("install")` platform. Desktop keeps the omnibox install icon as well; `preventDefault()` only suppresses the mobile mini-infobar. iOS, where no browser has an install API, shows the same Install button; tapping it opens a bottom sheet (`<dialog>`) with the 3-step Share → Add-to-Home-Screen instructions (2026-08-11). Until 2026-08-22 that path was gated on Safari alone (`isIOSSafari`, excluding `crios|fxios|edgios`), so iOS Chrome/Firefox/Edge fell through to the Chromium branch and, since no iOS browser ever fires `beforeinstallprompt`, saw no banner at all. An `iosBrowser` discriminator replaced it: every iOS browser gets the sheet, and its lead swaps to `iosSheetLeadChrome` or the generic `iosSheetLeadOther` via `dataset.i18n`. Chrome also gets its own steps 1–2 (`iosStep1Chrome`/`iosStep2Chrome`), because Safari's wording actively misleads there — Chrome's ⋯ overflow menu ends in a "Share Chrome" item that shares the App Store listing rather than the page, so the sheet names the address-bar share icon ("next to the website name") and says to scroll, the Add-to-Home-Screen row sitting near the bottom of the share sheet's action list. Third-party iOS browsers have been able to add a real standalone home-screen web app since iOS 16.4, and the manifest's `display: standalone` is what makes the icon launch in its own WebView rather than opening the default browser; `track("install")` now carries `browser` so the Chrome-iOS funnel is separable. Dismissal (✕), `appinstalled`, and an already-standalone launch suppress it (`installPromptDismissed`, 30-day re-arm). The app icons are the DelayBahn logo's train mark (DB-clock monogram in a train silhouette, since 2026-08-13; previously the boxed monogram), white on red, derived from `logo.png` by cropping the mark above the wordmark and thresholding its ink to a solid mask, the DB glyphs kept at the earlier icons' size for legibility.
- **Feature worktrees live inside the repo (2026-08-26)**: the `UserPromptSubmit` feature-workflow hook in `.claude/settings.json` puts every new feature worktree at `.claude/worktrees/<slug>` — that is where `EnterWorktree` already creates them, and the hook's fallback `git worktree add` (and its teardown `git worktree remove`) now names the same path instead of `../db_delay_predictor-<slug>`, which is why the location used to depend on which route a session took. No settings key does this: `worktree.location` is read only by Claude Code Desktop for SSH sessions, not by the CLI. Three older worktrees (firebase-auth, travellers, loadtest) are still outside the repo, so take the path from `git worktree list` rather than assuming. See log.md 2026-08-26.

## Not done / next candidates

- Meine Fahrten (2026-09-05): the stories hand-over carries one leg, never the whole trip; a round trip is filed as two independent rows, so removing one leaves the other; since 2026-09-11 a lifespan sweep (`_sweep_trip_verdicts`, 2 min after startup, then every 6 h) stores every past trip's check once the data reaches past its arrival day, so a trip nobody opened keeps its delay after the ~30-day delays window rolls past and the year tally stays complete (trips already out of the window that day are stored as "no data" for good; `TRIP_VERDICTS_PER_LOAD = 5` remains the on-page fast path); the tally only counts journeys filed through this site, so it is a record of what DelayBahn was used to book, not of all travel; no bookmark on past-mode cards; the upcoming cards no longer link out to bahn.de (removed 2026-09-05 on the user's ask), though the API still serves each row's stored `bahnUrl` and nothing reads it.

- Upstream demand is now instrumented: `bahn.de upstream:` rollup lines carry calls/min plus a per-source breakdown (`search` / `if-missed` / `walk` / `locations` / `nearby`), and `/health` mirrors it as `upstream_from_*`. Read those before proposing any further mitigation — the 2026-08-20 pass rejected heavier journey caching (≤8 % fewer upstream calls at any defensible TTL), a smaller `BAHN_MAX_CONCURRENCY` (a semaphore caps parallelism, not rate, and loosens when 429s return fast) and a bigger stale store, all on measurement.
- The if-missed derived cache (2026-08-20) is on trial: its hit rate was argued structurally, not measured, since replan keys appear in no log. If `if_missed_hits` stays low against `if_missed_misses`, or `caches.ifMissed` pins at its cap, revert rather than tune. If `if-missed=` in the rollup drops sharply and the 503 rate does not follow, the demand side is exhausted and the remaining answer is official DB API access.
- Cheap follow-up, deliberately out of scope on 2026-08-20: the if-missed gather slices `pending[:MAX_IF_MISSED_REPLANS]` before consulting the cache, so a cached answer still burns budget and the 4th+ tight transfer stays buttonless even when its answer is in memory. Applying the cap to misses only would add buttons at zero upstream cost.

- Per-country coverage is alerted since 2026-08-26 (`check_coverage` in `merge_delays.py`, see the decision above); what it cannot see is a run that never happens — a timer that stops firing, or a unit that fails before the merge (the `OnFailure` hook covers the latter). An external check of `maxDay` age — the ps083 watchdog already polls `/health`; `/api/coverage` carries the day — would close that.
- **Post-journey delay-report emails — built 2026-08-18, moved onto Delay Stories accounts and shipped 2026-08-26; sending for real since 2026-08-29.** Bell next to the book button is a toggle on the Firebase account: signed in → one press orders (uid + proven email stored with the snapshot in `data/reports/reports.db`), again → cancels; signed out → modal asks to log in, parks the journey in sessionStorage, `/login?next=…&reason=report` brings the visitor back and the order completes. The modal's copy was de-jargonised on 2026-08-26 (no "median"): the lead reads "After your journey we compare the past delay shown today with what actually happened – the report arrives by email about two days later." and the forecast row is labelled "Delay shown today" / "Heute angezeigte Verspätung". Since 2026-08-26 the receipt also carries the journey's "Auf bahn.de buchen" link (`#report-book`, the same `bahnDeUrl()` mask the card's own button uses, tracked as `book-bahn` with `via: "report-modal"`), so ordering a report is no longer a dead end for the booking. An account may keep **three** reports open at a time (`MAX_OPEN_PER_ACCOUNT`, 20 until 2026-08-26): a fourth order is refused with 409 `{"error": "too_many_open_reports", "limit": 3}` and the modal says so, naming the number the server sent and pointing at the lit bell as the way to free a slot; a repress on an already-ordered journey is deduped before the check and never refused, and orders made under the old cap are kept and still sent. Daily `pipeline/send_reports.py` resolves actuals via `delays.leg_delay_on_date` on D+2 (10-day timeout), freezes them into the row, sends the stacked forecast-vs-actual mail through `mailer.send_report` (Brevo SMTP, same relay as the login codes; `app/brevo.py` retired), and scrubs sent rows after 30 days. Live since 2026-08-29: SMTP_USER/SMTP_PASS are in the hetzner3 `.env` (2026-08-28), and the server-only `delaybahn-reports.service`/`.timer` is installed and enabled (oneshot as stripathi in the repo, `After=delaybahn-pipeline.service`, `OnCalendar=*-*-* 07:45:00 Europe/Berlin`, `Persistent=true`, `RandomizedDelaySec=5m`, OnFailure ntfy drop-in, ExecStart `uv run python pipeline/send_reports.py --send`; `.env` is read by `app.config` on import, so no `EnvironmentFile`). Verified the same day by a dry run (4 due, 3 would send, 1 waiting) and a `--test-to` delivery; the first real firing is 2026-08-30 ~07:48 Berlin, which clears the backlog. The `--sample`/`--test-to` fixture renders in English since 2026-08-29, and the footer carries only the unsubscribe and Impressum links since the same day (the postal address line was a CAN-SPAM habit, not a requirement for a transactional mail). Manual runs from root's shell go through `systemd-run -p User=stripathi -p WorkingDirectory=<repo> …`, because `sudo -u stripathi` inherits root's cwd and `uv` then fails on `/root/uv.toml`. Remaining canary: a production self-order and its D+2 mail.
- Leaderboard follow-ups: the sparkline's shaded period box (the selected day/week inside the 30-day line) has no legend, and each line has its own vertical scale so heights are not comparable between countries; the SNCF feed reports 84 % of stops at exactly 0 min (unchanged stops logged as on time rather than measured), which flatters France on any punctuality statistic.
- Add a startup sanity check (or an alert) for `data/delays.parquet` mtime / `maxDay` age, so a silently stalled pipeline surfaces instead of being noticed six days later.
- If traffic keeps growing past the 2026-08-13 surge: a Cloudflare cache rule for `/api/journeys` (responses already send `public, max-age`) would absorb popular-route repeats at the edge before they ever reach the box or bahn.de.
- Deploys run through `scripts/deploy.sh` since 2026-08-15 — the only server command Claude Code is pre-approved for (exact-match rule in settings.local.json, added by the user; blanket ssh was deliberately rejected). The commit-and-push skill's step 8 invokes it and forbids inlining ssh around it or editing it to widen its reach; changing what "deploy" means requires a reviewed diff of that script. Since 2026-08-17 its health check retries through the post-restart 502 race (`-f --retry 6 --retry-delay 3`) and fails the script on a health check that stays bad, instead of one immediate curl that printed Cloudflare's 502 body and exited 0; two exact settings.local.json rules also allow the ad-hoc health curl outside deploys. Every deploy and ops path targets the ssh alias `delaybahn_hetzner3` since the 2026-08-21 box move — `scripts/deploy.sh` (ae2bb15), then `scripts/delaybahn-watchdog.sh`, `pipeline/update_monthly_graphs.py`'s `commit_and_deploy()` and the feature-workflow hook in `.claude/settings.json`; the old box stays alive serving twiddle.pro and leader_board, so a stale target would have pulled and restarted a live but wrong machine. Since 2026-08-28 the script also runs `git submodule update --init --recursive` after the pull and aborts if `deutsche-bahn-data/scripts/create_monthly_data_release.py` is still missing — `git pull` alone moves the gitlink but never checks the submodule out, and the hetzner3 checkout's plain clone is what left the DE step crashing at import for the 2026-08-19..25 week. See log.md 2026-08-28.
- The docs gate in `.claude/settings.json` let a3e7645 through on 2026-08-14 — committed and pushed with no log entry and no `sw.js` buster bump, which is precisely what it exists to prevent. Replaying the same command shape against the hook body afterwards denies as intended and log.md was clean in both worktrees, so the miss has no explanation yet; watch whether it recurs. Two known sharp edges either way: it matches the raw command string, so any command whose *text* quotes the phrase is denied as if it were a commit (a heredoc appending to log.md, a grep for the gate itself), and it checks only that log.md is dirty — progress.md, feature_list.md and the buster sites are on the honour system. `.claude/skills/commit-and-push/SKILL.md` (2026-08-14) now spells the full sequence out so the honour-system half has something to follow.
- Guard against production drift: the Hetzner checkout is edited in place, and on 2026-08-07 a hero headline was found live that existed in no commit on any branch. Half-closed 2026-08-08 — the feature workflow in `.claude/settings.json` now ends with a deploy step (ssh to the box, pull origin/main, restart `delaybahn`), so prod can no longer sit *behind* origin after a merge. It deliberately stashes and restores server-side edits, so uncommitted work on the box still survives unnoticed; a cron reporting `git status --porcelain` there via ntfy would close the rest.
- Monthly graph refresh: since 2026-09-20 `pipeline/update_monthly_graphs.py --push` is meant to run from a dedicated clone on ps083 (`~/.local/share/delaybahn-graphs`, data/ symlinked, cron 09:30 on days 1–12; line in `~/.local/share/delaybahn-graphs.cron`, installation left to the user). Its --push path is still unexercised end to end (the September refresh was a dry run shipped by hand); watch the Oct 1 run and its ntfy. Separately, make the scatter's dot sample actually deterministic (ORDER BY train before sampling) — DuckDB row order varies per run, so the seed pins positions, not trains. The old-schema 2026-05/06/07 parquets sit in `data/monthly_processed_data/old-schema/` and can be deleted.
- Prefetch the adjacent result pages so earlier/later doesn't wait — but only on intent (`pointerenter`/`touchstart`), deduped against the click and gated on `bahn_api.healthy()`, so the cost tracks clicks rather than searches. bahn.de cannot return several pages in one request (one page + opaque `verbindungReference` cursors, no count field), and eager prefetching would roughly triple upstream search volume for the ~12 paging clicks per 100 searches measured on 2026-08-13. Revisit once the new `page` Umami event has a few days of data.
- Station suggestions can re-open after the field is left: the dropdown closes 150 ms after blur, but a slower `/api/locations` response then calls `toggle("open", …)` unconditionally, leaving a list floating over the results (it covered the "Frühere Verbindungen" button during a 2026-08-13 browser test; seen again 2026-08-15 floating over the search card at mobile width during the clear-button verification). Fix: ignore a resolved suggestion fetch when the input is no longer focused.
- Browser-verify the risk sort's within-tier margin rework (comparator-tested only, 2026-07-26) and the remaining 2026-07-19 additions (typed-station resolution, recent-station suggestions); tight-transfer warnings and shareable URLs were verified 2026-07-26.
- Further countries follow the same producer pattern (day parquets + one merge line + eva prefix); Austria joined 2026-08-14 via ÖBB HAFAS board polling, Italy 2026-08-15 via ViaggiaTreno run tracking.
- U-Bahn/tram/city-bus delay data (currently "nicht erfasst"): would need DELFI/gtfs.de GTFS-RT polling plus a bahn.de-extId↔IFOPT stop crosswalk and real storage growth; deferred 2026-08-02.
- Fare class (1st/2nd) is the last piece of bahn.de's traveller block still missing: the search is pinned to `klasse: KLASSE_2` and the deep link to `kl=2`, so a BahnCard 1st-class holder is priced for 2nd class (which is what bahn.de's own mask defaults to as well). Travellers themselves are done — a single traveler's age bracket shipped 2026-08-23 behind the advanced-options panel, the Verkehrsmittel filter 2026-08-25 beside it, and count + BahnCard discount + multi-row parties (capped at 5, bahn.de's own limit before group booking) on 2026-08-26. The two D-Ticket switches moved out into the main search row on 2026-08-26, leaving the panel four settings (travellers, Umstiegszeit, Verkehrsmittel, Zwischenhalte); each fragments the journeys cache on its own key and the traveller list is now the widest of them (age × count × discount per row rather than one of five brackets), so watch the upstream hit rate before adding a fifth.
- The bahn.de deep link's `r=` param has only ever been verified for one traveller. The multi-entry form the traveller picker now emits (`r=13:23:KLASSE_2:2,11:16:KLASSENLOS:1`) assumes commas separate the entries — the colon is taken and no other separator fits, but bahn.de's mask parser could not be read from this box (Akamai serves a stub for the SPA bundle, and the JSON `/angebote/stammdaten` it does serve says nothing about the URL form). Worst case the mask ignores `r` and opens on its default single adult; the route and date still carry. Worth one check from a browser that can reach bahn.de.
- An outside PR for a stdio MCP server (search_stations/search_journeys/coverage as agent tools) is expected from a visitor (email 2026-08-13; accepting reply drafted 2026-08-15). CONTRIBUTING.md (2026-08-15) records the terms it must meet: contributions under MIT — the repo's CC BY-NC 4.0 would otherwise leave only non-commercial rights to outside code — public API only with 429 backoff, no agent query loops, attribution link in tool responses.
- After the 2026-08-15 Puls de-risk, the header logo is the most DB-evoking element left (white rounded box, red DB-style monogram) — a redesign would touch logo.png, favicon.png, static/icons/* (five files) and the og:image reference together.
- Compensation checker only reaches back as far as the live parquet (~30 days); DB accepts claims up to 1 year — wiring up `data/monthly_processed_data/` archives (or HF backfill) would extend it. (The recent end is now covered live; this is the far end.)
- Live same-day lookups are Germany-only — IRIS has no Swiss or French stops, so a CH/FR leg on today's date stays "noch offen" until the next morning's build.
- delays.py in-process cache never invalidates; fine while the server restarts after each pipeline run.
- Akamai's 429s may omit `Retry-After` in practice, leaving the breaker's threshold path (5/60 s) to carry the load — tune `BAHN_CIRCUIT_*` against real logs after a few days. Cache/breaker/limiter state is per-process; adding uvicorn workers would silently multiply every budget.
- A hardening pass for the 2026-08-17→19 wholesale Akamai block (403 blocking of all three profiles for hours; ~45 % of searches 503ed, unalerted since only 429s page) shipped and was reverted the same day at the user's request (log.md 2026-08-19, both entries). Blocked windows still do not page and a sustained block is still re-probed every 30 s. The stale half of that pass is now known not to help: measured 2026-08-20 against 21,119 real searches, growing `STALE_MAX` from 256 to **unlimited** rescues 1.3 % → 2.1 % of the 503s and a 24 h `STALE_TTL` reaches only 3.9 %, because the store can hold only routes that succeeded before and an outage is mostly searching routes that never did. Do not re-raise those two knobs expecting relief.

## How to resume work

1. `uv sync`, then `uv run python pipeline/build_delay_db.py` and `uv run python pipeline/merge_delays.py` (day-incremental; skips existing downloads).
2. `uv run uvicorn app.main:app --port 8000`, open http://localhost:8000.
3. Read `feature_list.md` for scope, `log.md` for history, and `deutsche-bahn-data/AGENTS.md` for DuckDB patterns over the dataset.
